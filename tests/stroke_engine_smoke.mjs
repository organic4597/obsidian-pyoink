// Minimal smoke without Obsidian — run with node after esbuild not needed; inline logic test
// We can't import TS easily; skip or use built main. Simple assert file for CI later.
console.log('smoke placeholder ok');
